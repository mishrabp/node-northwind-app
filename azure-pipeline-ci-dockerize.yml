# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

# Pipelibe only build docker image and uploads to Azure Container Registry @113136 NTT Azure Accoount

trigger:
- development

pool:
  name: "MyBuildAgents"
  vmImage: ubuntu-latest

steps:
- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

#- task: Docker@2
#  displayName: Build Docker Image
#  inputs:
#    command: 'build'
#    Dockerfile: '**/Dockerfile'
#    tags: 'latest'
#- task: Docker@2
#  displayName: Login to Docker Hub (Public)
#  inputs:
#    command: login
#    containerRegistry: 'docker-hub-registry-public'
#- task: Docker@2
#  displayName: Push Docker Image to Docker Hub (Public)
#  inputs:
#    containerRegistry: 'docker-hub-registry-public'
#    repository: 'bibhupmishra/node-northwind-app'
#    command: 'push'

- task: Docker@2
  displayName: Push Docker Image to Azure Container Registry (Private)
  inputs:
    containerRegistry: 'docker-azure-registry-private'
    repository: 'nodeexceldataimporter'
    command: 'buildAndPush'
    Dockerfile: '**/Dockerfile'
    tags: 'latest'


